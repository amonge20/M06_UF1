//https://www.youtube.com/watch?v=RAu_lLyKEnQ
function tabla(){
    //Demana a l'usuari el numero de files i columnes
    let files = parseInt = prompt("Numero de files: ");
    let columnes = parseInt = prompt("Numero de columnes: ");

    //Multiplica la taula en la que 
    var taula = files * columnes;

    document.write("<table border>");

    for (let i = 0; i<files.length(); i++){
        let fila = document.createElement

    }
}